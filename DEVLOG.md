# 📓 DEVLOG — 일일 개발 기록

> 매일 한 줄씩. "오늘 무엇을, 왜 했는가"를 남겨 1년간의 성장 궤적을 기록한다.
> 상세 계획은 [ROADMAP.md](ROADMAP.md), 단계별 가이드는 [NEXT_STEPS.md](NEXT_STEPS.md).

| 날짜 | 구간 | 작업 | 비고 |
|---|---|---|---|
| 2026-06-29 | P0 | 레포 정상화(머지 충돌 해결, GitHub 최신본 일치) + 로드맵(ROADMAP.md)·DEVLOG 신설, 초기 MQTT 실험 아카이브 정리 | 포폴 프로젝트 시작 |
| 2026-06-29 | S1 | **설명가능성 추가**: `/predict`가 진단 근거(물리 신호 Top3) 반환 — LightGBM 내장 SHAP(`pred_contrib`), 무거운 shap 패키지 없이 경량 유지. `explain.py` 분리 + 한글경로 모델 로드 안정화 + pytest 4종 | 반도체 공채 타겟 확정, 로드맵 4개월 스프린트로 개편 |
| 2026-06-29 | A1 | **3D 디지털 트윈 착수**: `static/twin.html`(Three.js) — 3개 층 3D 슬래브·장비 박스·AGV 진단색 이동, 태블릿 OrbitControls(터치 회전/확대), 탭→설비 실시간 AI 진단 패널, KPI·경고 라이브. `realtime_server.py`에 `/twin` 라우트. 서버 기동·WS연결·렌더 검증 + 계약 테스트(test_twin) | 마스터 포폴/졸업작품 전시 비전으로 격상 |
| 2026-07-01 | A3 | **이상 설비 실시간 하이라이트**: 이상 AGV에 층 관통 경고 빔 + 맥동 경고 링 + 발광, 최우선 경고(최고 신뢰도) 자동 추적 버튼→카메라 자동 포커스 비행. 브라우저 렌더·포커스 동작 스크린샷 검증, 테스트 7종 통과 | 자율 일일 진행 모드 시작 |
| 2026-07-01 | docs | README 히어로에 3D 트윈 **움직이는 데모 GIF**(assets/twin_3d.gif) 추가 + 서버 미연결 안내 오버레이 | 포폴 대표 이미지 |
| 2026-07-02 | A2 | **팹 구조물 3D 보강**: OHT 오버헤드 레일(층별) + 스토커 타워 + 층간 리프트(층별 캐빈)를 전 층 관통 구조물로 추가. headless 브라우저 렌더 검증(에러 0), 테스트 7종 통과 | 디지털 트윈 현실감 강화 |
| 2026-07-02 | A4 | **설비 탭 패널 고도화**: snapshot에 AGV별 실시간 텔레메트리(진동·배터리·온도, 진단에 물리 커플링) + AI 판단 근거(주요 이상 신호) 추가 → 3D 선택 패널에 라이브 스파크라인 3종 + 근거 리스트 렌더. 헤드리스 렌더(에러 0)·라이브 서버 계약 확인, 테스트 9종 통과 | 관제 근거 가시화(직무 키워드: PdM·설명가능성) |
| 2026-07-02 | A5 | **태블릿 UX 마감**: 더블탭 → 홈 시점 부드럽게 복귀 제스처, 풀스크린 토글 버튼(전시 태블릿 크롬 숨김), 세로/좁은 화면(≤820·≤560px) 반응형 — 패널 축소·재배치·큰 터치 타깃. tablet 뷰포트에서 반응형 규칙·헤드리스 렌더(에러 0) 검증, 테스트 9종 통과 | 전시(11월) 대비 현장 조작성 |
| 2026-07-03 | B2 | **경고 등급(주의/경고/위험) 트리아지**: 진단 신뢰도(conf) 기반 3단계 등급을 서버 snapshot의 AGV·alert에 부여(0.85↑ 위험/0.60↑ 경고/그 외 주의) + KPI `by_level` 집계, twin.html 이상 경고 목록에 신뢰도순 정렬·색상 등급 배지 렌더. 등급 경계값·snapshot 계약 테스트 2종 추가(총 11종 통과), 라이브 서버로 필드 방출 확인 | 다수 경고 우선순위 트리아지(직무 키워드: 관제·경보 등급화) |
| 2026-07-04 | A6 | **오프라인 구동(Three.js 로컬 벤더링)**: import map을 CDN(unpkg)에서 `/static/vendor/three` 로컬 경로로 교체, three.module.js·OrbitControls.js를 repo에 포함 → 전시장 인터넷 없이도 3D 트윈 구동. 외부 CDN 미참조·로컬 서빙 계약 테스트(A6) 추가(총 12종 통과) | 전시(11월) 오프라인 안정성 |
| 2026-07-05 | C1 | **GitHub Actions CI 구축**: `.github/workflows/ci.yml` — push/PR 시 Python 3.11 셋업·의존성 설치·pytest 자동 실행. `pyarrow`(parquet 엔진) requirements 누락분 보강, README에 CI 배지 추가. 로컬 12종 테스트 통과 확인 | 실무 신뢰성·자동 검증(직무 키워드: 협업·CI/CD) |
| 2026-07-07 | B3 | **설비별 진단 이력 타임라인**: snapshot에 AGV별 최근 12틱 진단 추세(0 정상·1 주의·2 경고·3 위험 코드) 추가 + `diag_code` 헬퍼. twin.html 설비 탭 패널에 색상 셀 타임라인·범례·"최근 N틱 중 이상 M" 요약 렌더. 추세 계약·매핑 테스트 추가(총 13종 통과), 헤드리스 렌더(에러 0) 확인 | 설비 상태 변화 추적(직무 키워드: 관제·이력) |
| 2026-07-08 | B2 | **이상 추세(드리프트) 조기 감지**: `trend_direction`으로 진단 추세의 앞/뒤 절반 평균을 비교해 악화/개선/안정 판정 → snapshot에 AGV별 `trend_dir` + KPI `deteriorating`(플릿 악화 대수). twin.html: 설비 패널에 방향 배지(📉악화/📈개선), 경고 헤더에 "악화 추세 N대". 방향 판정·집계 계약 테스트 추가(총 14종 통과) | 고장 전 악화 조기 포착(직무 키워드: 예지보전 드리프트) |
| 2026-07-09 | viz | **관제 시각화 다듬기**: ① AGV 색을 **심각도별(정상 초록·주의 노랑·경고 주황·위험 빨강)**로 — '이상=전부 빨강' 벽 제거, 링·층관통 빔은 경고·위험만 ② AGV별 재생 타임라인 오프셋(`toff`, 황금비 분산)으로 **고장이 한꺼번에 몰리지 않게** ③ 설비 높이 변주 + 로드포트(FOUP) 디테일 + 헤미스피어/필 라이트로 입체감 ④ 범례 4단계화. README 대표 GIF 재생성. 헤드리스 렌더 검증·테스트 14종 통과 | 데모 완성도·현실성(직무 키워드: 관제 UX) |
| 2026-07-09 | viz | **실물 형상 리얼리즘**: ⑤ 3D AGV를 화살표 콘 → **실제 무인반송차 형상**(하부 대차 + 바퀴 4 + 상단 반투명 FOUP 포드 + 전방 진행등 + 상태 경광등/안돈)으로 재모델링 — 섀시는 금속 회색, 상태색은 경광등·진행등·포드에 ⑥ 설비를 와이어프레임 박스 → **솔리드 팹 장비**(베이스 플린스 + 베이별 색 변주 본체 + 상단 EFEM/팬필터 유닛 + 로드포트) ⑦ 트랙을 얇은 선 → **바닥 페인트 주행 레인 + 중앙 가이드라인** ⑧ 2D 도면(make_floorplan)도 오각형 카트 → **탑다운 실제 AGV 글리프(섀시·바퀴·FOUP 포드·진행등) + conf 기반 4단계 심각도 색** 통일(3D와 동일 컨셉). README 3D GIF·2D PNG/GIF 전부 재생성, 헤드리스 근접 렌더로 로봇 형상 확인·테스트 14종 통과 | 실제 현장·실제 로봇 완성도(직무 키워드: 디지털트윈 리얼리즘) |
| 2026-07-10 | B4 | **자산 건전도 지표(Health Index) + 정비 우선순위**: 순간 분류를 넘어 최근 진단 추세(심각도·신뢰도·드리프트)를 종합한 0~100 건전도 점수와 정비 트리아지 권고(정상 가동/모니터링/점검 권장/정비 권장/우선 대응)를 서버 snapshot에 부여 + KPI `maint_due`(정비 필요 대수)·`avg_health`(플릿 평균). twin.html: KPI에 '정비' 칩, 설비 패널에 건전도 바+권고 배지. README 2D 이미지 2장 중복 정리(정적 PNG 1장으로), 계약 테스트 추가(총 15종 통과) | 순간 진단 → 자산 건전도 관리로(직무 키워드: 예지보전 Health Index·정비계획) |
| 2026-07-10 | C4 | **시계열 데이터 계층 (SQLite, 무설치)**: 진단을 흘려보내고 끝이 아니라 **적재→집계→조회→보존**하는 경량 데이터 파이프라인 `telemetry_store.py` 신설 — 이상·저건전도 이벤트만 2Hz 선별 적재, `/api/stats`(등급별·층별·최다결함 Top5·평균건전도 롤업)·`/api/history?agv=`(설비별 진단 시계열) 조회 API, `max_rows` 보존정책. 기본 인메모리(세션 누적)·`TELEMETRY_DB`로 파일 durable. 3D 트윈 상단에 세션 누적 통계 HUD. 라이브 적재 검증(8초 246건)·계약/보존 테스트 2종 추가(총 17종 통과) | 순간 관제 → 데이터 파이프라인(직무 키워드: 시계열 적재·롤업·retention·조회 API, MQTT/TSDB 확장 기반) |
| 2026-07-11 | C2 | **Docker 패키징 + CI 검증** & **신뢰성·모니터링 계층**: ① `Dockerfile`(python:3.11-slim + 서버 전용 최소 의존성 `requirements-server.txt`) — `docker run` 한 줄 배포, 로컬에 docker가 없어도 **CI가 매 push마다 이미지 빌드+컨테이너 기동+엔드포인트 스모크 테스트**로 보증 ② **신뢰성 지표**: 이벤트 스트림에서 고장 에피소드 복원 → **MTBF·MTTR·가용도(Availability)** 계산(`/api/reliability`, 최저 가용도 Top5) — 설비 패널·HUD에 표시 ③ **Prometheus `/metrics`**(텍스트 포맷 게이지 10종: KPI·적재량·가용도·MTBF/MTTR) → Grafana 표준 연동점. 라이브 검증(플릿 가용도 74.8%·설비별 지표 렌더), 테스트 2종 추가(총 21종 통과) | 배포·신뢰성 공학·운영 모니터링(직무 키워드: Docker·CI/CD·MTBF/MTTR·Prometheus/Grafana) |
| 2026-07-11 | C4 | **시계열 분석 계층 — 다운샘플링·추이 차트·이력 조회·CSV 반출**: ① `trend()` **시간 버킷 롤업**(GROUP BY time bucket — 시계열DB continuous aggregate 개념) + `/api/trend` ② 관제 HUD 상단 중앙에 **플릿 추이 이중 라인차트**(경고 이벤트/버킷 + 평균 건전도, 20초 버킷 실시간 갱신) ③ 설비 탭 패널에 **적재된 이벤트 이력 리스트**(/api/history 화면 통합, 3초 스로틀) ④ `/api/history?fmt=csv` **CSV 반출**(Content-Disposition 다운로드) + 패널 CSV 버튼. 라이브 검증(버킷 집계·CSV 헤더·이력 렌더 스크린샷)·테스트 4종 추가(총 19종 통과), README 히어로 GIF 재생성 | 적재된 데이터 → 분석·반출 가능한 데이터로(직무 키워드: 다운샘플링·rollup·시계열 조회·리포팅 연계) |
| 2026-07-15 | docs | **대학원 컨택용 문서 정리**: README 성능 수치 불일치(공식 Validation 93.4% vs 과거 95.91% 표현) 정정, 교수 컨택용 1페이지 연구 브리프(`docs/professor_contact_brief.md`) 추가, 매일 개발 스프린트(`docs/daily_sprint_plan.md`) 추가, NEXT_STEPS의 완료/미완료 상태를 현재 구현 상태에 맞게 갱신 | KITECH 학연·제조AI/PHM 연구실 컨택 대비 신뢰도 정리 |
| 2026-07-15 | DQ1 | **로보틱스 데이터 QA/거버넌스 지표 추가**: `dataset_quality.py` 신설 — 로봇 학습 데이터 레코드의 필수 스키마, modality, annotation, QA 상태를 검증하고 schema valid rate·annotation coverage·QA pass rate·ingest success rate·rework rate 계산. `/api/data-quality`와 Prometheus `fab_data_qa_pass_rate` 추가, 단위 테스트와 endpoint contract 테스트 보강 | 현대차 제조로보틱스 데이터 엔지니어 공고 키워드(데이터셋 스키마·QA·거버넌스·운영지표)와 직접 연결 |
| 2026-07-22 | MLOps | **실시간 데이터 드리프트 감지 추가**: `drift_monitor.py` 신설 — live AGV 텔레메트리(`vib/batt/temp/health/conf`)와 이상 비율을 기준 운전 프로파일과 비교해 feature-level z-score, `ok/watch/drift` 상태, 재보정 권고를 산출. `/api/drift`와 Prometheus `fab_data_drift_*` 게이지 추가, 단위/endpoint 테스트 보강 | 데이터·AI 운영 역량 강화(직무 키워드: data drift, MLOps, 운영 AI 모니터링, 재학습 트리거) |
| 2026-07-24 | GOV | **모델 카드/모델 거버넌스 계층 추가**: `model_card.py` 신설 — 운영 중인 LightGBM artifact의 SHA256·파일 크기·피처 계약(30틱, x/y 제거, 249차원)·공식 검증 성능·한계·재학습 트리거를 JSON으로 구성. 추론 서버 `/model-card`, 관제 서버 `/api/model-card`, 문서 `docs/MODEL_CARD.md`, 계약 테스트와 Docker smoke check 추가 | 책임 있는 AI 운영 역량 강화(직무 키워드: model card, artifact lineage, feature contract, retraining trigger) |
| 2026-07-27 | DEPLOY | **Docker Compose 운영 실행 경로 추가**: `docker-compose.yml` 신설 — `docker compose up --build` 한 줄로 3D 트윈 관제 서버를 띄우고, `TELEMETRY_DB=/app/data/runtime/telemetry.db`를 named volume에 마운트해 진단 이력을 컨테이너 재시작 후에도 유지. `/api/snapshot` healthcheck와 배포 계약 테스트 추가, `docs/PROJECT_STATUS.md`로 최종 목표·현재 진행률·남은 작업 정리 | 포트폴리오 데모 완성도 강화(직무 키워드: deployment, reproducibility, durable telemetry, project planning) |
