# 🗺️ ServiceRobot_AI — 1년 성장 로드맵

> 이 프로젝트를 **"잘 만든 ML 스크립트"에서 "운영 가능한 예지보전(PdM) 플랫폼"으로** 1년에 걸쳐 진화시키는 기록입니다.
> 단순 정확도 자랑이 아니라, **데이터 수집 → 모델링 → 서빙 → 배포 → 관측 → 운영**까지 혼자 설계·운영했다는 것을 증명하는 게 목표입니다.

---

## 🎯 이 로드맵이 겨냥하는 직무

**제조/설비 도메인의 AI·데이터 엔지니어 (스마트팩토리 · 반도체 장비 · MLOps)**

- 단순 모델러가 아니라, **현장 설비를 이해하고 시스템을 만들어 운영하는 개발자**를 증명한다.
- CS 출신 개발자와의 차별점 = **물리적 설비·센서·엣지 리소스 한계를 아는 SW 엔지니어.**
- 그래서 로드맵은 의도적으로 **개발 중심**(컨테이너·메시징·DB·관측성·MLOps·배포)으로 구성한다.

---

## ✅ 이미 완성된 것 (Baseline)

| 영역 | 내용 |
|---|---|
| 데이터 파이프라인 | 원본 zip(4.2GB) → 피처 추출 + 공식 Train/Val 분리 |
| 모델 | LightGBM 경량 모델(2.8MB), 공식 Validation 93.4% / 랜덤분할 97.7% |
| 피처 엔지니어링 | 2D Flatten + 추세선(Drift) + FFT 주파수 + 누적 마모 신호 복원 |
| 일반화 | 절대좌표 암기(과적합) 제거, 처음 보는 로봇으로 정직하게 평가 |
| 서빙 | FastAPI `/predict`, `/health` (추론 1ms) |
| 관제 | Streamlit 재생 대시보드 + FastAPI/WebSocket 실시간 관제 웹앱 |

→ **모델링·서빙은 끝.** 아래는 "진짜 운영 시스템"으로 키우는 단계.

---

## 📅 12개월 로드맵 (월별 테마 · 일일 커밋 단위로 분해)

각 구간은 **독립적**이라 순서를 바꿔도 된다. 매일은 "한 구간의 하위 작업 1개"를 처리한다.
*잔디는 가짜 커밋이 아니라, 매일 쌓는 진짜 한 조각이다.*

| 구간 | 기간(목표) | 테마 | 증명하는 역량 | 상태 |
|---|---|---|---|:---:|
| **P0** | 0주차 | 레포 정상화 · 로드맵 박제 · 작업 루틴 확립 | 프로젝트 관리 | ✅ |
| **P1** | 1~2월 | Docker화 + pytest + GitHub Actions CI | 재현성 · 협업 · 실무 | ⬜ |
| **P2** | 3~4월 | 수집-추론 프로세스 분리 + **MQTT** 스트리밍 | Edge-Cloud · 메시징 | ⬜ |
| **P3** | 5~6월 | **시계열 DB**(TimescaleDB) 이력 저장 + **Grafana** 관측성 | 데이터 엔지니어링 | ⬜ |
| **P4** | 7~8월 | **SHAP** 설명가능성 + **데이터 드리프트 감지** | 실서비스 PdM 핵심 | ⬜ |
| **P5** | 9~10월 | **MLflow** 실험추적 + 자동 재학습 파이프라인 | MLOps | ⬜ |
| **P6** | 11월 | **ONNX** 변환 + 엣지 추론 벤치마크 | 경량화 · 임베디드 | ⬜ |
| **P7** | 12월 | **Docker Compose → Kubernetes** 배포 + CI/CD + 데모영상 | 배포 · 완성도 | ⬜ |

---

## 🔍 구간별 상세 (무엇을 · 왜)

### P0 — 기반 정비 (지금)
- [x] 머지 충돌 해결, GitHub 최신본과 로컬 일치
- [x] `ROADMAP.md` 작성 (이 문서)
- [x] 초기 MQTT 실험 코드(`asyncio_test.py`) → `src/archive/asyncio_mqtt_sketch.py` 아카이브
- [x] `DEVLOG.md` 시작 — 매일 한 줄씩 작업 기록

### P1 — Docker + 테스트 + CI
- `Dockerfile`(python:3.11-slim) + `docker-compose.yml`(api + dashboard)
- `tests/` : `pytest`로 피처 차원·`/predict` 응답 형태 검증
- `.github/workflows/ci.yml` : push 시 자동 테스트 + README CI 배지
- **왜:** "어느 환경에서도 동일 실행 + 자동 검증" = 실무 신뢰성 신호

### P2 — MQTT 실시간 수집-추론 분리
- `simulator.py` 분리 — 센서를 MQTT broker(mosquitto)로 publish
- 서버는 subscribe → 매 tick `booster.predict` 직접 호출(완전 라이브 추론)
- **왜:** 프로필의 *Edge-Cloud / MQTT* 서사를 코드로 실증. 면접 최강 데모

### P3 — 시계열 DB + 관측성
- TimescaleDB(or SQLite) : `readings` · `predictions` · `alerts` 테이블
- 매 예측 INSERT, Grafana 대시보드로 KPI·알림 이력 시각화
- **왜:** 수집→저장→조회→모니터링, 데이터 엔지니어 역량 추가

### P4 — 설명가능성 + 드리프트
- `shap.TreeExplainer` : "왜 배터리 저하로 판단했나" Top3 근거 표시
- 입력 분포가 학습과 벌어지면 경고(PSI/KL 기반 드리프트 감지)
- **왜:** PdM에서 "왜"와 "언제 모델이 늙는가"는 핵심

### P5 — MLOps
- MLflow로 실험·하이퍼파라미터·모델 버전 추적, 모델 레지스트리
- 새 데이터 들어오면 재학습 → 평가 → 자동 승격 파이프라인
- **왜:** 모델을 1회성이 아니라 **운영 자산**으로 다룬다는 증거

### P6 — ONNX + 엣지 벤치마크
- `robot_pdm_enhanced.txt` → ONNX 변환, 타 언어/엣지 추론
- CPU/엣지에서 지연·메모리 벤치마크 표로 정리
- **왜:** 경량화·임베디드 최적화 = 반도체 장비/엣지 직무 직결

### P7 — Kubernetes 배포 + 마무리
- Compose → k8s 매니페스트, CI/CD로 자동 배포
- 2~3분 데모 영상 녹화, README/이력서에 링크
- **왜:** "혼자 데이터부터 배포까지" 서사 완성

---

## 🔁 작업 루틴 (막히지 않게)
1. 한 번에 **하위 작업 하나만**. 끝나면 커밋.
2. 커밋 author는 항상 본인(`남주현 / nam3847@inha.edu`). 외부 co-author 트레일러 금지.
3. 각 작업 끝나면 이 로드맵 체크박스 + `DEVLOG.md` 갱신.
4. 막히면: 해당 작업만 떼어내 작은 예제로 먼저 동작 확인 → 본 프로젝트에 통합.

> 📌 세부 단계별 가이드: **[NEXT_STEPS.md](NEXT_STEPS.md)**
